const state={snapshot:null,projectId:null,taskId:null};
const token=document.querySelector('meta[name="workbench-token"]').content;
const $=(id)=>document.getElementById(id);
const esc=(value)=>String(value??"").replace(/[&<>"']/g,(ch)=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[ch]));
function notice(message,error=false){const el=$("notice");el.textContent=message;el.classList.toggle("error",error);el.hidden=false;clearTimeout(notice.timer);notice.timer=setTimeout(()=>el.hidden=true,5000)}
async function load(){try{const response=await fetch("/api/v1/snapshot",{headers:{Accept:"application/json"}});const body=await response.json();if(!response.ok||!body.ok)throw new Error(body.error?.message||`HTTP ${response.status}`);state.snapshot=body.data;if(!state.projectId||!body.data.projects.some(p=>p.id===state.projectId))state.projectId=body.data.projects[0]?.id||null;render()}catch(error){notice(`Snapshot failed: ${error.message}`,true)}}
async function action(payload){try{const response=await fetch("/api/v1/actions",{method:"POST",headers:{"Content-Type":"application/json","X-Workbench-Token":token},body:JSON.stringify(payload)});const body=await response.json();if(!response.ok||!body.ok)throw new Error(body.error?.message||`HTTP ${response.status}`);notice(body.data.message);await load()}catch(error){notice(error.message,true)}}
function render(){const data=state.snapshot;$("platform").textContent=data.platform;$("profile").textContent=data.profile;$("project-count").textContent=data.projects.length;const missing=data.doctor.summary.unavailable_core;$("health-title").textContent=missing?`${missing} core checks missing`:"Core ready";$("health-detail").textContent=`${data.doctor.summary.available} available · ${data.doctor.summary.unavailable_optional} optional missing`;document.querySelector(".pulse").classList.toggle("ok",!missing);$("projects").innerHTML=data.projects.map(project=>`<button class="project ${project.id===state.projectId?"active":""}" data-project="${esc(project.id)}"><span class="project-dot"></span><span class="project-name">${esc(project.name)}</span><span class="project-meta">${data.agents.filter(a=>a.project_id===project.id).length}</span></button>`).join("");document.querySelectorAll("[data-project]").forEach(button=>button.onclick=()=>{state.projectId=button.dataset.project;state.taskId=null;render()});const project=data.projects.find(p=>p.id===state.projectId);$("empty").hidden=!!project;$("workspace").hidden=!project;if(!project)return;$("project-name").textContent=project.name;$("project-path").textContent=project.path;const agents=data.agents.filter(a=>a.project_id===project.id);const worktrees=data.worktrees.filter(w=>w.project_id===project.id);const changes=data.changes.find(c=>c.project_id===project.id)||{changed:0,branch:"unknown",changed_files:[]};$("metric-agents").textContent=agents.length;$("metric-worktrees").textContent=worktrees.length;$("metric-changes").textContent=changes.changed;$("metric-branch").textContent=changes.branch||"branch unavailable";$("agents").innerHTML=agents.length?agents.map(agent=>`<button class="agent-card ${agent.id===state.taskId?"selected":""}" data-task="${esc(agent.id)}"><span class="agent-head"><span class="avatar">${esc(agent.agent_kind[0])}</span><span class="status ${esc(agent.state)}">${esc(agent.state)}</span></span><strong>${esc(agent.agent_kind)}</strong><small>${esc(agent.backend)} · ${esc(agent.id)}</small></button>`).join(""):'<div class="row"><span>No registered Agent tasks</span></div>';document.querySelectorAll("[data-task]").forEach(button=>button.onclick=()=>{state.taskId=button.dataset.task;renderTask()});$("worktrees").innerHTML=worktrees.length?worktrees.map(item=>`<div class="row"><strong title="${esc(item.path)}">${esc(item.branch||item.id)}</strong><span>${item.dirty?"dirty":"clean"}${item.managed?" · managed":""}</span></div>`).join(""):'<div class="row"><span>No linked worktrees</span></div>';$("changes").innerHTML=changes.unavailable?`<div class="row"><span>${esc(changes.unavailable)}</span></div>`:changes.changed_files.length?changes.changed_files.slice(0,8).map(file=>`<div class="row"><strong title="${esc(file)}">${esc(file)}</strong><span>changed</span></div>`).join(""):'<div class="row"><span>Working tree clean</span></div>';const caps=data.doctor.capabilities.filter(c=>c.scope==="core"||c.status==="unavailable").slice(0,8);$("doctor").innerHTML=caps.map(cap=>`<div class="row"><strong>${esc(cap.name)}</strong><span>${esc(cap.status)}</span></div>`).join("");renderTask()}
function renderTask(){const task=state.snapshot?.agents.find(item=>item.id===state.taskId);$("task-empty").hidden=!!task;$("task-detail").hidden=!task;if(!task)return;$("task-kind").textContent=task.agent_kind[0];$("task-id").textContent=task.id;$("task-state").textContent=task.state;$("task-state").className=`status ${task.state}`;const fields=[["Agent",task.agent_kind],["Backend",task.backend],["Project",task.project_id],["Worktree",task.worktree_id||"main"],["CWD",task.cwd],["Updated",new Date(task.last_event_at).toLocaleString()]];$("task-fields").innerHTML=fields.map(([key,value])=>`<dt>${esc(key)}</dt><dd>${esc(value)}</dd>`).join("")}
document.querySelectorAll("[data-action]").forEach(button=>button.onclick=()=>{const payload={action:button.dataset.action,project_id:state.projectId};if(button.dataset.backend)payload.backend=button.dataset.backend;if(button.dataset.agent)payload.agent_kind=button.dataset.agent;action(payload)});document.querySelectorAll("[data-task-action]").forEach(button=>button.onclick=()=>{if(!state.taskId)return;if(button.dataset.taskAction==="stop_agent"&&!window.confirm(`Stop registered task ${state.taskId}?`))return;action({action:button.dataset.taskAction,task_id:state.taskId})});$("refresh").onclick=load;$("doctor-details").onclick=()=>{const failures=state.snapshot.doctor.capabilities.filter(c=>c.status!=="available").map(c=>`${c.name}: ${c.reason||c.status}`).join("\n")||"All capabilities available";notice(failures)};load();setInterval(load,15000);
